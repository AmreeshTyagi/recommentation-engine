export function radians_to_degrees(radians: any) {
  var pi = Math.PI;
  return radians * (180 / pi);
}

export function degrees_to_radians(degrees: any) {
  var pi = Math.PI;
  return degrees * (pi / 180);
}